for $==for(int i = 0; i < $; i++)
pfor $==for i in range($):
wpp==web.whatsapp.com
shell $==system1($)
calc $==system2(python3 -c "print($)")
cprint $==printf(" : %d\n",$);
googlea==scholar.google.com.br
